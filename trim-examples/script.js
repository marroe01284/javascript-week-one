const carName = "   B   M   W";
//TRIM()
//will remove the soaces 
//and show a string

const trimmedCarName = carName.trim().replace(/[^a-zA-z0-9]/g,"");

console.log("trimmedCarName:", trimmedCarName);

function find(arr, value){
    for(let i = 0; i <arr.length; i++){

    }
    
}